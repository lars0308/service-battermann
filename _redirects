# Netlify prüft echte Dateien immer zuerst (index.html, leistungen.html, ...).
# Nur Pfade, die zu keiner Datei passen, fallen auf diese Zeile durch — z. B.
# eine im Sanity-Studio frei angelegte Unterseite wie /notdienst. page.html
# lädt dann per JS (assets/js/custom-page.js) das customPage-Dokument, dessen
# Slug zum aufgerufenen Pfad passt. 200 (kein 301/302): die Adressleiste
# behält den sprechenden Pfad, es ist kein sichtbarer Redirect.
/*  /page.html  200
